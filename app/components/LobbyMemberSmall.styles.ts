import { StyleService } from '@ui-kitten/components';

export default StyleService.create({
    container: {
        marginTop: 5,
        marginRight: 5,
    },
    avatar: {
        width: 50,
        height: 50,
    },
});
