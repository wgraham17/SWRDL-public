import { StyleService } from '@ui-kitten/components';

export default StyleService.create({
    root: {
        flex: 1,
    },
    scrollView: {
        padding: 20,
    },
    heading: {
        marginBottom: 10,
    },
    para: {
        lineHeight: 20,
        marginBottom: 10,
    },
    exampleWord: {
        marginVertical: 20,
    },
});
